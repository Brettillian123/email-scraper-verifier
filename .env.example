# Example overrides for local development.
# All values are commented out so defaults in code apply unless you opt in.
# Do not put secrets, private URLs, or tokens here.

# --- Runtime ---
# ENV=dev
# LOG_LEVEL=INFO

# --- DB / Cache ---
# DB_URL=sqlite:///dev.db
# RQ_REDIS_URL=redis://127.0.0.1:6379/0
# QUEUE_NAME=verify

# --- Identity (legacy app identity, optional) ---
# USER_AGENT=EmailVerifierBot/0.1 (+mailto:banderson@crestwellpartners.com)
# SMTP_HELO_DOMAIN=verifier.crestwellpartners.com

# --- Global rate limits ---
# GLOBAL_MAX_CONCURRENCY=12
# GLOBAL_RPS=6
# PER_MX_MAX_CONCURRENCY_DEFAULT=2
# PER_MX_RPS_DEFAULT=1

# --- Retries & timeouts ---
# VERIFY_MAX_ATTEMPTS=5
# VERIFY_BASE_BACKOFF_SECONDS=2
# VERIFY_MAX_BACKOFF_SECONDS=90
# SMTP_CONNECT_TIMEOUT_SECONDS=20
# SMTP_CMD_TIMEOUT_SECONDS=30
# RETRY_SCHEDULE=5,15,45,90,180

# --- R09 fetch/robots behavior (overrides only) ---
# FETCH_USER_AGENT=EmailVerifierBot/0.9 (+https://verifier.crestwellpartners.com/; contact: banderson@crestwellpartners.com)
# FETCH_DEFAULT_DELAY_SEC=3
# FETCH_TIMEOUT_SEC=5
# FETCH_CONNECT_TIMEOUT_SEC=5
# FETCH_CACHE_TTL_SEC=3600
# ROBOTS_CACHE_TTL_SEC=86400
# FETCH_MAX_RETRIES=2
# FETCH_MAX_BODY_BYTES=2000000
# FETCH_ALLOWED_CONTENT_TYPES=text/html,text/plain
