# requirements.txt

# === Core job queue ===
rq>=1.16,<2.0
redis>=5.0,<6.0
python-dotenv>=1.0,<2.0

# === Email verification helpers ===
email-validator>=2.0,<3.0
dnspython==2.6.1
aiosmtplib>=2.0,<3.0

# === HTTP scraping ===
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0
lxml>=4.9,<6.0

# === Reliability/utilities ===
tenacity>=8.2,<9.0

# === AI people extraction (O27) ===
openai>=1.52.0

# === Internationalization (O09) ===
Unidecode>=1.3

# === Database drivers ===
# Postgres driver (required when DATABASE_URL/DB_URL points at postgresql://...).
psycopg2-binary>=2.9.9,<3.0
# psycopg[binary]>=3.1,<4.0

# === Optional ORM ===
# SQLAlchemy>=2.0,<3.0

# === HTTP client dependencies (pinned for compatibility) ===
httpx==0.27.0
idna==3.7
tldextract==3.6.0

# === API server ===
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.6  # Required for form data (login forms)

# === Authentication ===
bcrypt>=4.0,<5.0

# === AWS (O26 test-sends / SQS, etc.) ===
boto3>=1.34,<2.0
